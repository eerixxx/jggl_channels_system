from django.apps import AppConfig


class TelegramChannelsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.telegram_channels"
    verbose_name = "Telegram Channels"

